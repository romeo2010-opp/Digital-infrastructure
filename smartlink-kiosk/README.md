
  # Station Kiosk for Attendants

  This is a code bundle for Station Kiosk for Attendants. The original project is available at https://www.figma.com/design/m1kx7fNxF6nlF9QhV9kdpW/Station-Kiosk-for-Attendants.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  